services:
  # ---- Django backend ----
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    container_name: vybe-backend
    command: >
      sh -c "python manage.py migrate &&
              python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
      - mongodb
    restart: unless-stopped
    networks:
      - vybe-network

  postgres:
    image: postgres:17.9-alpine
    container_name: vybe-postgres
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    networks:
      - vybe-network

  redis:
    image: redis:7.4.8-alpine
    container_name: vybe-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - vybe-network

  mongodb:
    image: mongo:8.2.6
    container_name: vybe-mongodb
    env_file:
      - .env
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped
    networks:
      - vybe-network


# creating named volumes here
# These persist data outside the container lifecycle

volumes:
  postgres_data:
  redis_data:
  mongo_data:

# --- Network ---
# A custom bridge network. All the services are on the same network
# can reach each other by service name.
networks:
  vybe-network:
    driver: bridge